const resul = (soma) =>{
    if(soma>=1 && soma<=2){
        return("resultado escrito, sugestoes")
    }
    else if(soma>=2 && soma<=3){
        return("resultado escrito, sugestoes")
    }
}

export default resul